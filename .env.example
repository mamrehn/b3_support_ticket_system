# Supabase-Zugangsdaten (öffentlich – der anon-Key ist per Design öffentlich, siehe README §3).
# Lokal: Datei nach `.env` kopieren und Werte eintragen.
# Produktiv (GitHub Pages): als Repository-Secrets hinterlegen, der Build bäckt sie ein.
VITE_SUPABASE_URL=https://DEIN-PROJEKT.supabase.co
VITE_SUPABASE_ANON_KEY=DEIN-ANON-KEY
